workflows:
  android-workflow:
    name: SabzTar APK Build

    scripts:
      - name: Build APK
        script: |
          ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/*.apk
