workflows:
  android-workflow:
    name: SabzTar APK Build

    scripts:
      - name: Build APK
        script: |
          cd $CM_BUILD_DIR
          gradle assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/*.apk
